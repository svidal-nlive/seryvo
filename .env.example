# ===========================================
# Seryvo Platform - Environment Configuration
# ===========================================
# Copy this file to .env and update the values
#
# Quick Start:
#   cp .env.example .env
#   # Generate a secret key:
#   openssl rand -hex 32
#   # Update DB_PASSWORD and SECRET_KEY below
#   docker compose up -d --build
# ===========================================

# ===========================================
# DATABASE CONFIGURATION (Required)
# ===========================================
DB_USER=seryvo
DB_PASSWORD=CHANGE_ME_STRONG_PASSWORD_HERE
DB_NAME=seryvo

# ===========================================
# APPLICATION CONFIGURATION (Required)
# ===========================================
# Generate with: openssl rand -hex 32
SECRET_KEY=CHANGE_ME_GENERATE_WITH_openssl_rand_hex_32

# Debug mode (set to false in production)
DEBUG=false

# Demo mode enables demo data and bypasses some auth requirements
DEMO_MODE=true

# ===========================================
# DEPLOYMENT MODE
# ===========================================
# For local development, use localhost URLs
# For production behind Traefik, use your domain

# Your domain name (for production mode)
DOMAIN=seryvo.vectorhost.net

# ===========================================
# LOCAL DEVELOPMENT PORTS
# ===========================================
# These are only used in development mode (without production profile)
BACKEND_PORT=8000
FRONTEND_PORT=3000

# Frontend API URLs (for local development)
VITE_API_BASE_URL=http://localhost:8000/api/v1
VITE_WS_URL=ws://localhost:8000/ws

# CORS origins (comma-separated list)
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# ===========================================
# DATA STORAGE PATH
# ===========================================
# Where persistent data is stored (postgres, redis)
# This path should contain /postgres and /redis subdirectories
# 
# Examples:
#   Local development: DATA_PATH=./data
#   VPS with centralized appdata: DATA_PATH=/home/user/docker/data/appdata/seryvo
#
# The compose file will mount:
#   ${DATA_PATH}/postgres -> PostgreSQL data
#   ${DATA_PATH}/redis -> Redis data
DATA_PATH=./data

# ===========================================
# STRIPE - Payment Processing (Optional)
# ===========================================
# Get keys from: https://dashboard.stripe.com/test/apikeys
# Test mode is FREE, production charges 2.9% + $0.30 per transaction
#
# Leave empty to disable payment features
STRIPE_PUBLISHABLE_KEY=pk_test_YOUR_PUBLISHABLE_KEY
STRIPE_SECRET_KEY=sk_test_YOUR_SECRET_KEY
# Webhook secret (optional, for async events)
STRIPE_WEBHOOK_SECRET=whsec_YOUR_WEBHOOK_SECRET

# ===========================================
# RESEND - Email Notifications (Optional)
# ===========================================
# Get key from: https://resend.com/api-keys
# FREE tier: 3,000 emails/month, 100/day
# IMPORTANT: You must verify your domain at https://resend.com/domains
#
# Leave empty to disable email features
RESEND_API_KEY=re_YOUR_API_KEY
RESEND_FROM_EMAIL=Seryvo <noreply@yourdomain.com>

# ===========================================
# WEBPUSH - Push Notifications (Optional)
# ===========================================
# Generate keys with: npx web-push generate-vapid-keys
# This uses the W3C Web Push standard - completely free
#
# Leave empty to disable push notifications
VAPID_PUBLIC_KEY=YOUR_VAPID_PUBLIC_KEY
VAPID_PRIVATE_KEY=YOUR_VAPID_PRIVATE_KEY
VAPID_MAILTO=mailto:admin@yourdomain.com

# ===========================================
# REDIS (Internal - usually no changes needed)
# ===========================================
REDIS_URL=redis://seryvo-redis:6379/0
