# Seryvo Frontend Environment Variables

# API Configuration
# Set to 'true' to use real FastAPI backend, 'false' for mock data
VITE_USE_REAL_API=true

# Backend API URL (used when VITE_USE_REAL_API is true)
# For local development:
VITE_API_BASE_URL=http://localhost:8000/api/v1

# For production (via Traefik):
# VITE_API_BASE_URL=https://seryvo.vectorhost.net/api/v1

# WebSocket URL for real-time features
VITE_WS_URL=ws://localhost:8000/ws
# VITE_WS_URL=wss://seryvo.vectorhost.net/ws

# Mapbox token for maps (optional - falls back to OpenStreetMap)
# VITE_MAPBOX_TOKEN=your_mapbox_token_here
